# MITAKO Agent 更新日志

本文记录对外可说明的关键变更。真实交付、部署与验收以 `docs/delivery/`、`甲方沟通交付文档/`、`我方内部开发文档/` 为准。

## 2026-08-18 三包发布与文档收口

状态：当前有效。此轮未改四场景审核业务代码，复用 0816 已冻结的 8 份正式报告，不重复调用真实模型或完整 API E2E；新增验证只覆盖发布脚本单测、三包内容/哈希/隐私边界及解包启动冒烟。

- 发布物由“双 ZIP”改为三份：甲方运行包、内部研发代码包、四场景离线验收证据包。
- 内部研发包不再重复携带约 300MB 的大体量样本与敏感离线图片；新增证据包集中保存 8 份 HTML、119 个 WebP、索引、manifest 与 SHA256。
- 甲方运行包新增客服可读更新说明与三包边界说明；敏感身份、支付和用户图片仍不进入运行包。
- 根 README、内部研发入口、甲方交付入口和验收 HTML 统一补齐当前业务契约、黄金经验、技术对接、8 案质量索引和版本说明链接。
- 开发者更新日志说明 Prompt/Schema、确定性决策、媒体预处理、JKAdmin、API/Web 同源与已知联调边界；甲方更新说明改用客服可理解的语言。
- 两个打包脚本默认仍执行完整预发布门禁；只有发布者显式传入 `-ReuseValidatedAcceptanceEvidence` 时，才允许纯文档/打包发布复用冻结证据。
- 发布回执同时记录三份 ZIP 的提交号、字节数、SHA256、条目数和适用范围，三个资产必须随同一个 Release 发布。

## 2026-08-17 四场景审核主链重建（发布候选）

状态：代码、全仓自动回归、百度云 Lite/3.7 鉴权、运行时目录、JKAdmin 模型管理、0816 八案密封运行与解封、API/Web 报告和甲方说明已收口；当前只剩双 ZIP 验包和私人/公司双仓发布。八案采用甲方授权的“资料中出现过但从未正式模型运行”口径，不外推生产准确率。

- 将商品有伤、发错货、漏发货、未成年人退款资料拆为四份黄金审核经验和独立场景规则；旧通用模板、旧正负槽位和历史交付说明不再拥有当前发布门禁资格。
- 将 Prompt、Schema 和模型目录分别集中到 `prompts/visual_review/scenes/`、`prompts/visual_review/schemas.py` 和 `configs/model_catalog.py`，旧 POC 入口只保留兼容导入。
- 模型只抽取中立可见事实；材料齐全性、订单/包裹对账、仓库终核、SOP 决策、人工路由、脱敏和公开报告改由服务端确定性处理。
- 商品有伤新增真实初次拆包动作、开箱八字段、服务端综合结论、逐伤点状态、严重结构伤窄例外、有效展示窗口离镜和严格三段人为致损链；普通伤缺开箱改为补件/复核，不再机械判负。
- 发错货按角色、系列、版本、物理形态和包含部件对账；仅数量短缺切换漏发。漏发拆分用户证据路线、商品构成、仓库核验和最终事实依据，静态三类材料只进入仓库人工核对。
- 发错货与漏发货只共享中立原子组件，模型响应分别固定 `wrong_item_observation_v1` 与 `missing_item_observation_v1`；跨场景版本在严格解析边界拒绝，不再由同一个顶层 Schema 混用。
- 未成年人退款恢复五类材料逐项审核、身份证正反面、订单和支付双门槛、承诺书字段、手机号归属、未满 10 周岁支付过程说明，以及高置信未满 9 周岁的额外人工关注；材料问题不再输出支持或不支持退款。
- 新增 `review_inventory` 和四场景 `material_readiness`，记录业务输入、媒体状态、页数/时长、SHA-256、重复附件、齐全结论、确定性、理由和唯一下一步。
- 图片逐张转 WebP、不拼图，超过 4K 才缩至最长边 2560；视频按 100 MB、2K、24 FPS、6 Mbps 阈值评估完整时长 VP9 代理。运行媒体可优先落到 `E:\MITAKO_Agent_Runtime`。
- 部署依赖检查复用真实媒体运行目录；转码运行在 E 盘时不再因 D 盘剩余空间不足而误报失败，并输出实际检查路径。
- 审核模型目录收敛为百度 `gemini-3.5-flash-lite` 默认档与 `gemini-3.7-flash` 显式高质量候选；3.6 和其他历史模型退出运行时选择面。两档均使用 High/High、完整原生视频 `fps=1`，不设置 `maxOutputTokens`；3.7 不进入自动兜底。全片 1 FPS WebP 回退默认关闭。
- 百度 Lite 与 3.7 最小请求已返回 HTTP 200；原生完整视频 A/B 未证明 3.7 能修复细微伤点。对 587966 的身份锚定 1 FPS 受控实验中，3.7 在不接收人工时间点或配色答案时命中 `36.97–41.97s`，Lite 未能隔离目标；但 3.7 完整批次需 11 次调用和约 31.9 万 tokens，因此默认仍为 Lite 原生视频，3.7 只作显式高质量档，批次 1 FPS 默认关闭。
- 修复批次模型字段别名导致的候选帧丢失；最终归并直接重看候选帧与官方身份锚点，并禁止把袋装/包装首次出现误写为实体商品首次可辨识展示。身份命中与主视频伤点命中保持独立。
- 视频速度、剪辑和拼接拆分为技术信号、视觉语义与关键链影响；无可信原速锚点时速度黄标未知，低置信或单一编码异常不得直接判负。
- 报告改为场景专属双层结构：首层只显示整体建议、证据确定性、材料状态、符合条件的严重标识和唯一下一步；详情提供原片时间点、场景原子字段与人工复核说明，不展示原始 JSON、模型渠道、Key、Prompt 或个人字段原值。
- 待确认口径不再使用“支持或不支持用户诉求”的万能句；商品有伤、发错货和漏发货分别列出本场景需要继续核对的事实，API、Web 与 HTML 共用同一建议数据源。
- 修复租户隔离、审核任务租约 fencing、失败/重试用量累计、工作台持久幂等、公开文本运单/路径脱敏和客户包运行时模块校验。
- 新增 JKAdmin“审核策略”治理页：客服主管可调整审核强度、1 FPS 采样预算、开箱角色预检、视频/图片压缩阈值、编码偏好和高成本回退开关；每次发布/回档强制填写至少 6 个汉字理由，按租户记录账号、角色、时间、版本与变更历史。
- 保留原有四场景业务规则治理作为唯一场景规则入口，不复制第二套业务配置；底层指令、结构化字段契约、访问凭据和上游私有参数不向客服主管开放。
- 新增 0817 甲方业务验收说明、8 份 HTML 报告质量索引和 Java/私有化技术对接说明；甲方页面统一使用成本档/高质量档等人话文案，不暴露内部渠道或本机路径。
- 正式工单的技术重试收敛为最多 3 轮；新增追加式轮次审计，失败、成功和租约过期不再被下一轮清空。视频送审版继续按原片哈希持久复用，重试不重复转码。
- 同源送审视频转码增加跨线程、跨进程单航班锁和二次缓存检查；并发工单只生成一次持久送审版。Workbench 回填改为合并 v2 衍生媒体清单，保留原片/送审版关系，不再覆盖或降级清单。
- 根 README 重写为 Java 研发可直接理解的模型请求、非 AI 处理、数据流、前端职责、接口和部署说明；甲方入口只标当前综合契约、主线进度和黄金经验为 `CURRENT`。
- 最新本地证据：全仓 `1113 passed + 100 subtests passed`，前端生产构建完成 `1924 modules transformed`；内部预发布全门禁通过；八案桌面报告、四份代表性移动报告及业务规则桌面/移动共 `14/14` 浏览器验收通过，签名图片、视频跳秒、全屏和 `206 Range` 已实际验证。旧清单和不具备当前资格的结果仍由发布校验拒绝。

## 2026-07-28 事实结论与人工复审闭环

- 商品有伤的可见事实与形成原因分离；成因不确定不再覆盖明确伤情。
- 补充图片只有在已采纳、证据分达标且同物关联成立时确认可见伤情，不越权推断责任。
- 非阻断连续性疑点改为可抽检，缺材料优先补件；服务失败、实质冲突和权威核验仍必须复审。
- HTML 标题、事实结论、人工建议和流程建议统一读取 `advisory_assessment`，关闭局部口径矛盾。
- 真实 API 任务 `RJ-765B0EA12FB04FE9` 返回 positive / 0.65、伤情 confirmed、成因 indeterminate、人工复审 optional。
- 新增 0728 甲方人话版、内部升级日志和双 ZIP 最新文档门禁；0724 说明降为历史记录。

## 2026-07-23 审核建议契约、三级复审与可选 HTML

- 新增统一 `advisory_assessment`：事实结论、未校准证据分数、三级人工复审、流程建议、风险信号和业务边界。
- 材料缺口改为优先 `request_more_material`，不再仅因缺材料强制占用人工席位。
- 连续离镜默认 3 秒只建议补连续原视频；短暂遮挡作为抽检信号，离镜本身不等于剪辑、调包或欺诈。
- 网页默认生成 HTML；正式 API 可通过 `output_options.include_html_report=false` 只返回 JSON。
- JSON-only 任务不写报告文件、不返回虚假 URL，报告路由明确返回 409 `review_report_not_requested`。
- HTML 报告首屏新增事实结论、证据分数、复审级别、流程建议、风险信号和禁止自动业务动作说明。
- 新增 API 使用说明、Java 字段映射、甲方非技术 HTML 和内部升级日志，并纳入双 ZIP 发布门禁。

## 2026-07-22 订单资料与官方商品图按需接入

- 官方图缓存不再写入源码目录，默认跟随 `MITAKO_DATA_DIR`，并允许用 `REVIEW_PRODUCT_IMAGE_CACHE_DIR` 指向独立磁盘或容器卷。
- 盘点 1,422 个新资料工单目录：1,129 个含订单快照，1,127 份按批次/场景/工单唯一关联并经 SHA-256 复验，2 份异常隔离。
- 订单适配器合并重复 SKU 数量，只保留本单 SKU、名称、规格、应发数量、物流引用、相关抽赏规则和主图引用；隐私、价格、整份商品库和人工标签不送模。
- 官方商品图改为单审核任务按需读取，争议行项目优先，默认最多 6 张、硬上限 12 张；长视频最多在首/中/末 3 个主审核分段附图，避免重复计费。
- 下载链路支持 HTTPS/主机/DNS/重定向/大小/像素/MIME/魔数/解码校验、JPEG 压缩、DNS 超时和有 TTL/容量上限的缓存。
- Gemini 与 OpenAI 兼容通道以内联图片接收官方参考图，不依赖供应商 Files URI；官方图与用户视频/补图使用不同证据角色。
- 网页单文件夹、网页批量父目录和正式审核 API 共用同一链路；HTML 报告新增官方图可用状态和文字订单基线降级说明。
- 明确一个物流单号不能推导包裹-SKU 映射；缺少分包、赠品/特典和证据覆盖时，漏发货继续降级为人工复核。
- 抽赏/赠品规则不完整时禁止发错货、漏发货确定性结论；公开 HTML 直接展示系统订单基线，完整物流单号不送模。
- 客户运行包补齐订单适配、官方图模块和 Pillow 依赖；自动化结果：视觉审核 114/114、正式审核服务 46/46。

## 2026-07-20 独立逐帧审核与资料状态分层

- 删除模型审核链路中的 WebP 时序拼图分支；所有供应商协议统一逐张接收带帧号和时间戳的 JPEG 独立帧。
- 连续性通道单次请求硬限制为最多 24 帧，接口采样计划明确返回原生与兼容协议均为 `individual_frames`。
- 模型 payload 忽略内部图片覆盖字段，防止后续调用者重新把拼图送入判定链路；HTML 缩略图仅供人工浏览。
- 未成年人资料把“已提交”“材料质量”“视觉字段一致性”拆开；部分可读材料不再被误报缺失，也不再阻断五项一致性检查。
- 144989 最新正式任务 `RJ-2E0B44A7C5C942E6`，20/20 图片、五类材料、五项一致性检查和 14/14 自动验收通过，仍保持人工复核与权威接口边界。
- 617911 最新正式任务 `RJ-51F5E65338A449E5`，213 帧、ffprobe 完成、连续性最大 24 帧分段；结果保持 `review`，未完整返回的逐帧观察被显式记为覆盖缺口。
- 正式 API 与网页单文件、单文件夹、批量父目录真实请求 7/7；视觉测试 98/98、审核服务 44/44、私有部署 API 14/14、前端构建通过。

## 2026-07-20 订单 SKU 快照、证据分权与报告安全

- 新增甲方 `order_info_snapshot.json` 对账工具，按 Case 编号、业务场景和样本类型三层校验；默认只预检，只有 `--apply` 才复制，冲突或无法唯一关联时拒绝写入。
- 本轮 230 份来源快照对应 226 个唯一 Case；严格按 Case、场景和正负类型复核后仅 6 份形成唯一证据链，211 份在原样本集中无目标，13 份场景或类型不一致。旧逻辑错误复制的 12 份已按源/目标哈希审计后安全撤销。
- 网页单工单文件夹、网页批量文件夹和正式 API 均可接收订单快照；审核前仅提取 SKU、商品名、规格、应发数量和商品主图引用，不把用户、地址、价格或人工标签送入模型。
- 发错货与漏发货以版本化 `fulfillment_baseline` 作为应发基准；没有 SKU/应发清单时明确输出基准缺失，不能凭视频单独认定发错或漏发。
- 商品有伤报告拆分采样边界、媒体取证、开箱完整性、主体连续性、主视频损伤和补充图片关联，避免把“抽到了首尾帧”误写成“完整视频已审”。
- 自动分类策略改为服务端批准版本注册表；调用方只能选择 `policy_ref`，不能通过请求参数降低证据门槛。
- 公开报告与证据图片改为短时、路径绑定的签名 URL；生产环境可强制固定签名密钥，未配置时 readiness 失败。
- 模型调用失败仍保留已产生的调用数、Token、时延和估算成本，不再以零成本掩盖失败消耗。

## 2026-07-20 未成年人资料字段一致性审核

- 修复五类材料齐全后直接输出高置信 `positive/pass` 的误导性聚合逻辑；仅完整但未核字段时固定进入 `review`。
- 新增身份年龄、监护关系、承诺书签署主体、订单支付、手机号实名五项视觉字段一致性检查。
- 新增受控 Schema、图片覆盖确认、敏感字段白名单丢弃和一致性失败关闭门禁；公开结果不保存姓名、号码、金额或 OCR 原文。
- 明确视觉一致性与权威真伪验证分层，身份证、运营商实名、订单和支付统一标记 `customer_integration_required`。
- 更新 HTML 报告，展示五项状态、风险码、证据图片编号和权威验证边界。
- 144989 正式 API 盲测 14/14，20/20 图片、五项检查全部执行；身份年龄和监护关系 matched，其余证据不足项保持 review。
- 正式 API、网页单文件、网页单工单文件夹和网页批量工单真实提交模式 7/7 通过。

## 2026-07-17 单件、文件夹与批量审核交付

- 网页工作台新增批量工单模式：选择父目录后按一级子文件夹拆分案件，分别隔离素材、执行审核并生成报告。
- 正式 API 单附件、多附件和同 `batch_id` 多案件汇总，与网页单文件、单文件夹、批量父目录共同纳入真实请求回归。
- 提交模式真实 E2E 7/7，通过任务、批次、文件接收数、报告数和页面路由绑定多重校验。
- 新增 0717 甲方人话版 HTML、我方提交模式升级日志，并将 144989 与提交模式证据加入双包强制清单。

## 2026-07-17 未成年人资料全覆盖审核

- 修复含视频案件只把前 4 张补充图片送模、导致后续出生证明、支付记录、运营商发票和补件身份证被误报缺失的问题。
- 新增未成年人资料独立分批审核管线、逐图覆盖校验、结构化遗漏重试和视频过程证据通道。
- 按《未成年人退款 2.0》实现五类材料规则，明确户口本/出生证明二选一，并区分“已提交候选材料”和“主体一致性已验证”。
- 送模文本新增手机号、证件号、银行卡号、邮箱和敏感字段遮盖；公开报告不展示个人号码或 OCR 原文。
- 正式 HTML 增加五类材料、图片覆盖、证据编号、验证状态、过程视频与业务边界面板。
- 144989 正式 API 盲测 11/11：20/20 图片、五类材料 5/5、视频取证完成、PII 不回显，结论为可进入人工一审而非自动退款。

## 2026-07-17 四样本审核工程与私有化部署增强

- 商品有伤普通档提升为 1 FPS 全时间轴三通道质量底线，Strong 统一为 2 FPS、最多 1800 帧。
- 新增版本化 claim scope 和默认保守的分类策略，区分视觉证据结论与甲方材料规则资格；任何结果均不自动执行拒绝、退款、补发或定责。
- 修复分段末帧被误写为视频结束、局部结论拼接和公开报告时间线矛盾；新增确定性全局摘要与聚合警告。
- 补齐 ffprobe 固定路径、真实媒体探测、Docker 镜像、完整 Compose、审核 readiness 与 Nginx 800MB 基线。
- 真实 E2E 捕获并修复内部 127.0.0.1 请求误经代理、约 602 秒 503 和孤儿推理问题；内部调用强制直连并记录每次尝试。
- 受保护审核 API 恢复 Token、调用数和估算成本，浏览器工作台与公开 HTML 继续脱敏。
- 四样本五任务正式 API 盲测 11/11 门槛通过；四个定向样本只用于缺陷回归，不用于宣称总体准确率。

## 2026-07-17 网页目录上传与客户包边界修复

- 修复网页端目录上传误读 `__MACOSX`、`._*.mp4`、点号隐藏文件和系统元数据的问题。
- 浏览器预过滤与后端内容签名校验形成双层防护；扩展名与实际内容不一致的媒体不会进入抽帧。
- 单个无法解码的视频改为逐文件隔离；同目录有效视频或图片继续审核并返回跳过原因。
- 上传目录改为 UUID 原子隔离，并通过 20 路并发回归。
- 页面分别显示用户选择、浏览器提交、服务端接受和跳过数量；有跳过时显示部分完成状态。
- 客户包没有内置样本时自动隐藏样本按钮，批量内置样本改为全部成功才返回成功。
- 甲方网页报告移除 Token、成本和内部调用统计，正式 API 与内部运维观测保持不变。
- 新增 0717 甲方 HTML 整改报告、内部升级日志和发布包冷启动门禁依赖。

## 2026-07-16 视觉审核因果、连续性与履约对账升级

- 将商品有伤审核拆为主审核、主体连续性、损伤因果三个独立模型通道；任一专项通道漏帧、重复帧、超时或结构不完整均降级人工复核。
- 新增动作前/中/后同对象同部位因果证据链，避免仅凭“有伤”直接推断物流、原包装或用户操作责任。
- 新增逐帧主体状态时间线，并在 HTML 报告回链离镜前、离镜起点和重新入镜关键帧。
- 新增发错/漏发 `fulfillment_baseline` 与 `evidence_coverage` 契约、应发/实收跨分段对账和证据不足保护。
- 模型请求不再包含原始文件名或标签目录；置信度拆分模型自评、成因、履约对账、可见覆盖和规则降级后决策分。
- 采样计划和报告按三通道统计模型调用、Token、时延与估算成本；OpenAPI、Java 样例和双方文档已同步。
- 修复服务重启后 HTML 报告抽帧图片 404，并用受限运行时媒体目录保证历史报告可回放。
- 新增甲方人话版 HTML，明确内部研发包与甲方测试版的区别、本轮九项更新、可测内容、演示数据和待联调边界。
- 甲方测试版编译运行时补齐损伤因果、主体连续性、履约对账、专项通道和输入隔离模块；打包前后强制检查跟踪文件无未提交改动。
- 修复客户运行时脱敏误改公开 API 字段的问题：保留 `estimated_channel_calls` 契约，只过滤具体供应商、密钥与内部路由信息。

## 2026-07-15 0714 客户反馈闭环与发布门禁升级

- 完成 0714 用户端、坐席台、审核服务和运营后台反馈整改，新增 28 项专项回归。
- 修复抽赏问题误判物流、附件审核摘要误触发人工、商品有伤材料清单被“外包装/外包”规则误伤。
- 修复管理员全队列 500、排队期间消息、真人首响、历史审核回填和用户切换竞态。
- 完成用户端入队、坐席二次确认接手、人工回复到达用户端的真实浏览器闭环。
- 全链路 API/通信/角色/浏览器 54/54，严格鉴权 12/12，私有化 API 13/13。
- 真实运行 `sample_003`：16 文件、约 106 MB，模型成功返回自评分、成本与 HTML 报告；该自评分不代表已证明的业务准确率。
- 预发布脚本兼容 `.venv`/`venv`，加入 0714、前端隔离和数据目录隔离门禁；`.venv/` 禁止提交。
- 新增甲方人类可读的 0714 HTML 更新日志、文字版整改说明、内部 0715 升级日志与对抗式 HTML 验收报告。
- 发布物拆分为内部研发源码包和甲方演示测试包，并以同一 Git 提交号、证据哈希和解压后冷启动验收保证可追溯性。

## 2026-07-11 独立审核服务、私域 Agent 与交付工程升级

- 新增面向甲方系统调用的独立审核 API，覆盖商品有伤、发错货、漏发货和未成年人退款材料核验。
- 支持异步任务、批次并发、幂等提交、任务进度、部分失败隔离、结构化审核结果和 HTML 报告。
- 增加媒体元数据预检与抽帧计划，支持动态策略及后台配置 1 FPS、2 FPS；大文件采用对象存储直传、转码、抽帧后审核的生产方案。
- 审核结果补充置信度、证据链、缺失材料、人工复核建议、耗时、Token 与成本估算。
- 私域 Agent P0 增加群事件、商品事件、分层、受控运营动作、舆情预警和客服转接契约；企业微信、飞书、CRM/CDP 等真实系统仍需联调。
- 完成客服 Agent 0709 反馈修复、视觉结果回写、人工服务期间实时分析和多阶段回归。
- 重写甲方交付索引、内部系统清单、Java 部署指南、测试指南和验收清单，并重新生成当前 FastAPI OpenAPI。
- 客户验证包改为白名单交付，排除内部文档、历史误导性材料、源码、密钥、数据库、缓存和原始样本。

## 2026-07-06 项目整理与客服交互验收收口

- 重写项目 README、更新日志、环境变量示例和 Git 忽略规则，修复根文档乱码问题。
- 补齐用户端客服交互验收报告：`tests/reports/customer_chat_acceptance_20260706.html` 与 `.md`。
- 用户端新增 6 类演示用户：延期 180 天、抽奖质疑、破损售后、正常在途、新用户、未成年人退款。
- 订单筛选语义收口：“需优先处理”只展示风险/人工关注信号，“在途/待发”展示普通物流与待出库状态。
- 商品工具列表扩充为 11 条，覆盖现货、预售、盲抽、规格、破损售后、未成年人退款等典型咨询入口。
- 清空对话后的欢迎流程改为分阶段 Loading：接入AI客服、同步服务记录、展示推荐或新用户引导。
- 验证通过：`npm run build`、`python -m py_compile business_api.py main.py`、`python scripts/dual_system_smoke_test.py`。

## 2026-07-05 客服 Agent 与视觉审核交付路径收敛

- 旧版 Companion、陪伴、文字冒险、角色扮演服务线从主系统入口、API、构建入口和回归范围中剥离。
- 客服 Agent 保留 MBTI/服务人格，但定位调整为“专业、同理、有边界的服务型助手”。
- 新增甲方与我方两套独立文档系统，分别面向甲方决策/客服/Java 开发与我方正式研发/测试/实施。
- 视觉审核工作台继续作为三大优先场景 POC：开箱视频/发错货、商品有伤、未成年人资料审核。
- 当前甲方真实后台、客户系统、订单、仓库、财务和 IM 接口仍按接口契约与联调适配层推进，不伪装为已真实对接。

## 2026-07-04 视觉审核模型选型与报告重做

- 聚焦 Gemini 3.5 Flash 重新设计单样本审核报告，避免泄露人工标签给模型。
- 将开箱视频、多视频、补充图片、用户诉求、订单 SKU、主数据要求合并为证据包。
- 移除传统 YOLO 路线在当前三大审核场景中的主线地位，改为多模态理解模型输出结构化审核结论和证据链。
- 增加模型选型对比思路：同一 Case 多次测试稳定性、耗时、Token、成本、结论、证据时间戳。

## 2026-07-02 客户验证包安全与验收修订

- 客户验证包统一通过 `scripts/package_release.ps1` 生成，不建议手工压缩源码仓库。
- 打包门禁检查本地配置、数据库、源码、测试产物、临时目录和客户不可见术语。
- 视觉审核工作台关闭内部模型渠道、Prompt、Key、调试参数等客户不可见信息。
- 主客服链路增加普通回复净化、订单/物流业务语义校验、严格鉴权下 WebSocket 会话存在性检查。

## 2026-06 客服 Agent 验证版

- 建立用户端客服、VIP客服坐席台、运营后台、转VIP客服、坐席接单、坐席回复、升级处理、补偿审批和服务记录链路。
- 建立视觉审核工作台，支持本地视频上传、公开视频链接解析、抽帧策略配置和复核摘要输出。
- 建立客户对接物料清单、测试指南、部署指南和验收清单。

## 本地数据说明

本地配置文件、数据库、运行时记忆、日志、测试报告、截图、视频样本和临时目录只用于开发与验证，不进入 GitHub 和客户验证包。提交前必须确认 `.env`、`data/`、`viking_memory/`、`logs/`、`tmp/` 未被暂存。
