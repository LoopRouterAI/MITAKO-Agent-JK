# 升级日志：事实结论与人工复审闭环

状态：当前有效

更新时间：2026-07-28 10:00:00 +08:00

覆盖关系：本日志是 0727 代码修正与 0728 发布资料的工程入口；继续遵守 `MITAKO售后审核Agent业务认知基线-20260727.md`、0724 样本口径与物流上下文、0723 审核建议契约。0724 对外说明保留为历史，不再作为当前版本入口。

## 1. 反馈与根因

0724 版集中暴露了三类问题：审核结果普遍落在 0.5-0.7 并全部进入人工复核；报告不同区域对“是否复核”和“视频是否完整”给出矛盾信息；明确可见伤情会因为成因或商品连续性不确定而被覆盖成 `review`。

根因不是单一提示词过严，而是聚合层混合了四个不同问题：事实是否成立、成因与责任是否确定、是否需要人工复审、最终业务如何处置。报告又从多个局部对象取值，导致同一任务出现不同口径。

## 2. 代码修正

| 模块 | 修正 |
|---|---|
| `poc/visual_review_poc/damage_causality.py` | 分离可见伤情事实与形成原因；成因不确定不得覆盖明确伤情。 |
| `poc/visual_review_poc/object_continuity.py` | 连续性不确定作为独立风险；只在依赖连续性的自动分类规则中阻断。 |
| `review_service/advisory_assessment.py` | 将非阻断连续性信号降为 `optional`，保留服务失败、实质冲突和权威核验为 `required`。 |
| `poc/visual_review_poc/report_renderer.py` | 标题、事实、人工建议和流程建议统一读取 `advisory_assessment`；补件不再显示必须 VIP 复核；媒体状态转为人类可读文案。 |
| `docs/delivery/review-advisory-api.md` | 明确事实、责任、人工复审和业务处置四层契约。 |

补充图片只有在三项同时满足时才确认“可见伤情存在”：审核通道已采纳、证据置信度不低于 0.8、已建立同物关联。该规则不推断伤情形成时间或责任。

## 3. 输出与业务边界

- `positive`：当前证据支持事实诉求，不代表自动退款或平台责任。
- `negative`：当前证据不支持事实诉求或命中甲方已批准的材料规则，不代表自动拒绝。
- `review`：证据冲突或不足，不能被业务处置标签替代。
- `required / optional / not_required / request_more_material`：分别表示必须复审、可抽检、无需复审和先补材料。
- 最终退款、补发、换货、补偿或拒绝由甲方系统、SOP 与授权人员执行。

## 4. 真实请求证据

真实案件 `588609` 通过正式 API 三轮盲测，只提交用户诉求、3 份用户媒体和安全订单快照，没有提交目录标签、`annotation.json`、`reply.json` 或最终处置。

最终任务 `RJ-765B0EA12FB04FE9`：

- 事实结论：`positive / 0.65`；
- 可见伤情：`confirmed`；
- 成因：`indeterminate`；
- 人工复审：`optional`；
- 流程：`continue_by_customer_policy`；
- 媒体取证：完成；
- HTML：可读取，标题、事实、复审和流程无矛盾，不包含内部媒体状态码。

## 5. 回归证据

```powershell
.venv\Scripts\python.exe -m unittest discover -s tests\visual_review -p "test_*.py"
.venv\Scripts\python.exe -m unittest discover -s tests\review_service -p "test_*.py"
.venv\Scripts\python.exe scripts\check_private_deployment_api.py
npm run build
.venv\Scripts\python.exe scripts\check_documentation_release.py
```

0728 复验结果：视觉审核 145/145、审核服务 79/79、私有化 API 14/14、前端生产构建通过。API 报告为 `tests/reports/private_deployment_api_smoke_20260728_125733.json`。

## 6. 部署与联调

1. 同一 Git 提交构建主服务与视觉服务，重启两个进程。
2. Java/业务系统只调用主服务 `/api/v1/review/*`，不要直接依赖内部视觉接口。
3. 先检查 readiness，再做单案、批次、JSON-only 和 HTML 四类请求。
4. 每个请求保留 `job_id`、`request_id`、策略版本、耗时、调用次数、Token/成本状态和失败原因。
5. 甲方路径不固定；调用方从任意本地路径或对象存储取得文件后上传，服务端不得依赖研发机 `E:` 盘。

## 7. 发布资料修正

此前代码和 ZIP 已更新到 0727，但对外入口仍把 0724 HTML 当作最新版，内部首页也停留在 0723。这是发布资料缺陷，不是运行代码回退。本轮新增：

- `甲方沟通交付文档/0728事实结论与人工复审闭环更新说明.html`；
- 本日志；
- 甲方/内部文档首页最新入口；
- 双 ZIP 必需文件与证据哈希门禁；
- 独立解压验收对两份 0728 文档的存在性检查。

## 8. 剩余风险与下一入口

当前已关闭“机制性全 REVIEW”和报告口径矛盾，不代表 23 个历史样本已经达到生产准确率。下一入口只有一个：双方冻结四场景各 25 案的黄金集，未成年人另建材料集；通过正式 API 输出混淆矩阵、确定性覆盖率、人工复审率和矛盾率，再按场景校准阈值。
