# 系统清单与代码地图

版本：2026-07-16

## 1. 系统总览

```text
用户/客服浏览器
  -> FastAPI 主服务
     -> 客服 Agent
     -> 人工客服与后台
     -> 审核编排服务 -> 视觉审核服务
     -> 私域 Agent P0
     -> SQLite/运行时数据

甲方 Java/网关
  -> /api/v1/auth/*
  -> /api/v1/review/*
  -> /api/v1/private-domain/*
  -> /metrics 与 /api/v1/ops/snapshot
```

## 2. 模块清单

| 系统 | 代码入口 | 存储 | 外部依赖 | 当前边界 |
|---|---|---|---|---|
| 用户端客服 | `src/App.jsx`、`src/hooks/useChatSSE.js` | 会话/订单样例 | 主服务 SSE | POC 可演示 |
| 客服 Agent | `agent.py`、`agent_llm.py` | 会话记忆 | 文本服务、业务适配 | 不做最终业务动作 |
| 人工客服 | `src/desk/`、`handoff_service.py`、`handoff_store.py` | SQLite | WebSocket/SSE | 接手、回复、转交、结案 |
| 运营后台 | `src/admin/`、`admin_service.py`、`admin_store.py` | SQLite | 主服务 API | 角色、队列、质检、审批、报表 |
| 鉴权 | `auth/` | SQLite | JWT/SSO 适配 | 生产应接甲方账号体系 |
| 审核编排 | `review_service/` | `review_service.db`、素材目录 | 视觉服务 HTTP | 多案件、批次、重试、报告 |
| 视觉审核 | `poc/visual_review_poc/` | 临时帧和报告 | 视觉服务凭证 | 预处理和证据审核 |
| 私域 Agent | `private_domain/` | `private_domain.db` | 企微/商品/客服契约 | 群级 P0，不直接群发 |
| 业务准备度 | `business_api.py`、`business_readiness_service.py` | 样例数据 | 甲方接口适配 | 本地流程和契约验证 |
| 运维与 SLA | `ops_service.py`、`sla_worker/` | 日志/指标 | Prometheus | 本地可观测，生产待告警接入 |
| 发布包 | `scripts/package_release.ps1` | ZIP | PowerShell/Python | 白名单打包和敏感词门禁 |

## 3. 审核服务数据流

```text
Java 提交 metadata + files
  -> ReviewCaseMetadata 校验与标签隔离
  -> 保存任务与素材
  -> worker 调用视觉服务 /api/review-folder
  -> 图片压缩 / 视频抽帧 / 24 帧分段
  -> 聚合证据、置信度、Token、成本
  -> 写回任务
  -> Java 查询 job 或 batch
  -> 获取公开 HTML 报告
```

核心文件：

- `review_service/router.py`：OpenAPI 路由。
- `review_service/schemas.py`：案件、采样、任务和批次模型。
- `review_service/service.py`：创建任务、调用视觉服务、聚合和恢复。
- `review_service/store.py`：SQLite 任务状态和批次聚合。
- `poc/visual_review_poc/model_selection_e2e.py`：媒体准备、分段调用和结果聚合。
- `poc/visual_review_poc/specialized_model_pass.py`：连续性/损伤因果专项分段与完整性校验。
- `poc/visual_review_poc/object_continuity.py`：逐帧主体时间线和离镜保护。
- `poc/visual_review_poc/damage_causality.py`：动作前/中/后因果链和成因边界。
- `poc/visual_review_poc/fulfillment_reconciliation.py`：应发/实收清单和跨包裹对账。
- `poc/visual_review_poc/report_renderer.py`：公开报告。
- `poc/visual_review_poc/sample_evaluation.py`：离线样本表解析与探索性一致率，不参与生产推理。

本轮已将视觉工作台服务从 1200 余行拆到 1000 行以内。`main.py`、`agent.py` 仍是历史大型模块，后续应按路由域和 Agent 节点逐步拆分；交付前不做无行为收益的高风险重写。

## 4. 私域 Agent 数据流

```text
企微群消息契约
  -> 兴趣/许愿/风险快筛
  -> 群标签与健康状态
  -> 高风险禁推 + 客服任务

商品/库存事件契约
  -> 扫描群画像
  -> IP/角色/库存/风险匹配
  -> 待审核触达候选
```

当前没有真实企微发送、用户级长期记忆、完整 RAG、A/B 或自动群发。

## 5. 数据目录

| 数据 | 默认位置 | 是否进 Git/客户包 |
|---|---|---|
| 运行数据库 | `data/` | 否 |
| 上传素材 | 运行时数据目录 | 否 |
| 临时抽帧/报告 | `tmp/`、视觉报告目录 | 否 |
| 脱敏演示数据 | `mock_data.json`，客户包改名 | 仅客户可见样例 |
| 环境变量 | `.env` | 否 |
| 环境模板 | `.env.example` | 是，空值 |

## 6. 生产替换点

- SQLite -> PostgreSQL。
- 本地线程池 -> Redis/RabbitMQ/Kafka + 分布式 worker。
- 应用服务器上传大文件 -> 对象存储直传 + 转码回调。
- 本地 JWT 账号 -> 甲方 SSO/网关。
- Mock 业务适配 -> 甲方订单、库存、CRM、企微和飞书测试接口。

替换应保持 `/api/v1/review/*` 和 `/api/v1/private-domain/*` 的外部契约稳定。
