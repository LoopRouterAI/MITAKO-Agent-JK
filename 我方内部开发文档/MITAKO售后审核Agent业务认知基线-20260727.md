# MITAKO 售后审核 Agent 业务认知基线

状态：当前有效

更新时间：2026-08-05 04:56:55 +08:00

覆盖关系：本文件是售后视觉审核业务理解的当前唯一入口。它覆盖此前把“看见伤情”“判断伤情成因”“SOP 是否认可证据”“平台是否承担责任”“最终退款或补偿”混成一个标签的认知，也覆盖“补充特写图可以改写主视频所见”“未成年人资料默认必须接权威接口、否则每单转人工”“业务动作不由 Agent 执行，所以 Agent 不给明确建议”以及“靠路径名、文本关键词和分段标签修正整案结论”的旧口径；0723 三级复审契约、0724 样本字段与物流传递实现继续有效。

## 最新记录入口（2026-08-05 04:56:55 +08:00）

状态：当前有效

0803 反馈最终确认：证据事实、SOP 建议、人工复审和甲方业务动作是四个独立输出。`business_action_allowed=false` 只限制服务自动执行退款、换货、补偿或拒绝，不限制 Agent 给出明确的 `positive/negative` 证据建议。`human_review=optional` 表示按风险抽检，不得在 API 摘要或 HTML 下一步中继续写成逐单 VIP 复核。

商品有伤默认 `core_sop` 只用关键事实形成建议：完整主视频未见所诉损伤可给不支持倾向；主视频有可回链的明确损伤可给支持倾向。离镜、媒体取证、损伤成因、补充图同物关联和中等证据分数保留为风险或抽检信号，但不能组成“必须全部通过”的默认总门禁。若视频不合规规则先命中，则仍优先输出不支持的 SOP 倾向。严格全门槛策略仅作为独立版本保留。

未成年人资料中，空白模板和示例图不满足材料要求；普通运营商账户截图只作辅助证据；清晰、已填写且类型可识别的证件与业务材料可计为已提交，未知质量项标黄而不是伪造缺件。编辑风险必须有图片级、位置明确的证据或独立检查交叉印证；EXIF 缺失、压缩、扫描、转发和重复泛化描述不单独判红。身份证、户口本、出生证明和护照均可进入身份/监护关系视觉初审，公开报告继续脱敏。

四个中立 Case 已通过真实接口盲测：商品有伤正、负各一案和未成年人完整资料一案与推理后加载的复核标签一致；未成年人缺件一案正确输出具体补件且不先占用人工。推理请求未包含人工标签、最终回复、管理员消息、正负目录名或隐藏资源文件。该结果只证明定向缺陷闭环，不代表生产准确率；正式指标仍以冻结黄金集为准。

## 最新记录入口（2026-08-01 04:56:00 +08:00）

状态：当前有效

事实层与规则层必须只有单向关系：事实聚合只输出伤情、连续性、同物关系、资料一致性等证据维度，不得提前改写整案标签、置信度或人工路由；最终分类建议只由服务端批准的版本化 SOP 生成。0731 默认商品有伤策略面向降低人工工时：媒体取证不可用、未拍特写或同物关联不足等非致命信号降低证据解释力，但不再全部串成“必须同时通过”的默认门槛。需要全部门槛的场景使用独立严格策略版本，不得把严格模式偷偷变成默认模式。

当前默认口径：完整送审时间轴已审核、主视频未见所诉损伤、模型明确给出 `not_supported`、没有证据冲突且离镜未超过已批准阈值时，可以输出明确负向 SOP 建议；补充图若已完成审核则作为补偿/复核参考，不因“图中可见伤”自动推翻开箱视频口径。该建议仍不自动拒绝、退款或赔付。

## 最新记录入口（2026-08-01 03:50:00 +08:00）

状态：当前有效

0731 修偏结论：审核输入隔离必须按结构化字段和文件来源执行，不得扫描普通用户文本中的“审核不通过、拒绝”等词来猜测金标；`manifest.tag`、目录名、人工标签和最终处置不得参与场景识别或送模。整案结论必须来自可回链的结构化证据与服务端批准的 SOP，不得使用分段标签投票、中文损伤关键词、包装类型词表或报告层文本替换来“校正”模型。

专项能力只负责对应证据维度。连续性或损伤成因分段局部失败时，成功证据继续有效，缺口只保持该维度未知；只有主审核本身不完整时才阻断整案确定结论。报告渲染器只能转义和排版，不得把“外包装”改写为“包装”，也不得把所有 `review` 猜成“证据不足、必须 VIP 复核”。同一损伤成因跨分段重复出现时，公开报告每类只保留证据最强的一项，完整分段审计留在内部诊断。

视频能力边界：离镜时长按送审帧的源时间戳输出，并同时给出采样分辨率及上下界；它是证据强度信号，不单独证明调包。媒体取证可以识别容器、码流、时间戳和明显变速风险，但在没有原始时钟或可信基准时不能精确反推出恒定加速倍数，必须输出 `unknown`，不得猜测。

未成年人资料能力边界：支持身份证、户口本、出生证明和护照的资料类型、签发国家/地区、身份角色、年龄与监护关系视觉一致性初审；护照号、身份证号、手机号和 OCR 原文不进入公开报告。国别或字段不可读时输出 `unknown`。视觉一致不等于法定真伪，当前未接入的权威接口默认不阻断标准模式，也不得伪装已经验真。

稳定性能力：分段任务按配置并发，遇到 408/429/5xx 或软失败会降低后续波次并发；成功波次可逐步恢复。重试遵守 `Retry-After`，否则指数退避；只在已配置且认证完整的渠道间兜底，非重试错误不切换渠道。公开 API/HTML 不显示渠道、模型密钥或内部路由细节。

四个真实中立案件均已完成结构化结果和 HTML：`case_001/002/004` 为 `partial_specialized`，`case_003` 为 `complete`；离镜最长估计分别为 49.37、68.87、80.93、111.40 秒。`case_001` 395 帧、墙钟约 498 秒，其余三案合计约 1395 秒，说明高密度双专项全量检查的主要成本来自模型调用量。正式异步 API 任务 `RJ-1FF3D0A5AE80416C` 成功处理 105.99MB/16 文件，公开 JSON 无模型渠道明细且 HTML 可访问。以上证明工程路径已生效，不代表样本标签命中或生产准确率；黄金集仍按盲测口径评估。

## 最新记录入口（2026-07-31 01:35:00 +08:00）

状态：当前有效

0731 商品有伤反馈确认：审核必须先分别回答“主视频看到了什么”“补充图片看到了什么”“按照 SOP 是否认可这组证据”，再给事实分类建议。补充特写图即使清晰且能证明瑕疵存在，也不得把主视频的 `not_visible/uncertain` 改写成 `confirmed`；报告必须同时展示两种来源及其关联状态。

当前商品有伤 SOP 口径为：缺少或不合规的开箱视频，分类倾向可为 `negative`，含义是“现有证据不满足商品有伤 SOP、当前不支持诉求”，不是“客观证明商品无伤”，也不自动拒绝用户。补充图片可供客服参考最低档补偿或人工复核，但不能替代合规开箱视频。视频加速本身不构成不合规；只要一镜到底、关键开箱过程完整、争议部位可慢放看清，仍可判为合规。拼接、跳切、关键过程缺失、争议商品长时间离镜且重新入镜同一性未确认，才影响合规性。

四案最新口径：589098 为“主视频无法识别微小划痕，补充特写支持瑕疵存在；按 SOP 倾向不支持，但可参考最低档补偿”；615437 为“主视频未见所诉伤痕且商品离镜过长；按 SOP 倾向不支持，补充图不能覆盖开箱视频不合规”；593840 为“Agent 找到人工可能漏看的疑似视频伤帧”，应标为证据分歧并回看前后片段，不得为贴合历史标签直接判负；617109 的多个视频应合并审查，加速不明显或前段加速不影响后段原速、完整展示损伤时，可按合规视频继续判断。

样本依据优先级为：甲方最新明确复核结论 > 双方冻结的黄金标/归一确认单 > 历史 `annotation.json` > `reply.json` 中的最终经营处置。`VCHK_黄金标批量归一确认单_20260730(参考用).xlsx` 自身仍存在 617109 等字段归一与最新口头复核不一致，当前仅作参考，不可直接充当模型答案。推理输入只允许中立目录、用户材料、审核时点可用的用户消息、清洗后的订单/SKU/物流基准；目录名、人工标签、管理员结论和最终处置不得进入模型。

验证状态：规则与报告语义已建立自动回归；真实四案 E2E、浏览器媒体加载和黄金集准确率仍需在本轮后续步骤完成，完成前不得宣称生产准确率。

## 最新记录入口（2026-07-30 22:45:00 +08:00）

状态：当前有效

0730 反馈确认：未成年人资料审核的默认目标是完成五类必交材料清点、跨材料可见字段一致性初审和图片风险提示，不应依赖甲方当前不存在的身份、运营商或支付验真接口。外部在线验真默认关闭，视觉一致性存疑为黄色抽检项；只有明确字段冲突、字段比对技术失败、多重疑似编辑证据或显式启用严格策略时，才要求人工复审。该口径不宣称证件已完成法定真伪验证，也不授权 Agent 自动退款或拒绝。

单案图片继续采用动态容量：40 张以内标准分批，41-200 张扩展分批，全量处理后再聚合；隐藏文件和 `annotation.json`、`reply.json`、`manifest.json` 等评测答案在上传层隔离。真实性风险同时参考 EXIF 是否存在和视觉疑似编辑线索；缺少 EXIF 只标黄，不单独证明造假；单个疑似编辑信号也只标黄，多项相互印证才标红并阻断。

面向客服的报告以“事实结论、证据分数、复审等级、流程建议”为首屏，使用绿/黄/红交通灯语义；五类材料和一致性检查可点击回看证据，风险为空时折叠或明确写“未发现”，末尾给出客服下一步。未成年人报告隐藏商品订单基线、商品有伤策略等跨场景模块，并把内部英文状态转换为人话。

验证证据：真实正式 API 任务 `RJ-62DE227AA5234D70` 盲测 144989，20/20 张图片和 1 个有效视频完成，五类材料 5/5，默认权威验真关闭，`human_required=false`，14/14 验收通过，视觉服务处理 32.79 秒、16 次模型调用、131,906 Token、内部估算成本 0.052941 美元；51/62 张动态容量任务 `RJ-073A540EFD844C13`、`RJ-88CB542816B44F77` 均实现 100% 接收与处理。完整自动回归为 269 passed、39 subtests；桌面/手机报告无横向溢出，36/36 张报告媒体可加载。以上证明工程能力，不代表黄金集生产准确率。

## 最新记录入口（2026-07-28 19:59:59 +08:00）

状态：当前有效

本轮进一步关闭 0727 反馈暴露的工程问题：单案资料不再固定截断为 40 份；40 份是正常处理软上限，41-200 份自动分批并完整聚合，超过部署安全上限才返回结构化 413。当前请求内的结构修复与逐张恢复后仍未覆盖全部资料时，输出 `technical_processing_incomplete + system_retry`；调用方可受控重跑整案且应计入可能重复的模型成本，不得伪装成用户缺材料或生成虚假置信度。

网页工作台和正式 `/api/v1/review/*` 现统一经过同一后处理、策略和建议生成链。商品有伤默认启用已版本化的“事实分类建议”策略：证据清楚可建议 `positive`，完整审查未见主诉伤情可建议 `negative`，证据冲突或关键基准缺失才为 `review`；所有建议仍保持 `business_action_allowed=false`，不直接决定退款、补偿或拒绝。HTML 中的帧图和原视频改为经主服务按任务代理访问，避免把内部工作台地址或本机路径发给浏览器。

仍有效的旧结论：事实、成因责任、人工复审和经营处置必须分层；发错货、漏发货需要订单/SKU/包裹基准；未成年人权威真伪核验仍需甲方接口；生产准确率仍需黄金集。失效内容：把 40 份当作单案硬上限、把技术未处理完解释为用户缺材料、商品有伤默认全量保守 `review`。

验证证据：真实任务 `RJ-7B52916F28364AE8` 在首次提交中完成 62/62 资料接收和 62/62 处理；正式 API/网页提交模式 7/7、私有化 API 14/14、审核服务 90/90、视觉审核 151/151。该证据证明工程链路和容量契约，不代表黄金集准确率。

## 1. 商业目标

甲方需要的不是一个替代客服说话的聊天机器人，而是一套能降低客诉证据审查工时的审核服务：自动读取订单、SKU、物流、用户媒体和必要对话，输出可回链的事实结论、证据强度、疑点、人工复审等级与人类可读报告，再由甲方业务规则决定退款、补发、换货、补偿或拒绝。

当前人力排序为：商品有伤 > 漏发货 > 发错货 > 未成年人退款。有伤、漏发、错发需要查看图片/视频并与仓库协作，约占售后审核人力的 80%；未成年人案件量较少，单次审核不重，但材料经常多轮补交，约占 20%。因此 Demo 首要价值指标不是“能否生成报告”，而是：在可审案件中给出明确事实建议，减少无意义的全量人工复核，同时不把证据不足包装成确定结论。

## 2. 必须分开的四层结论

| 层级 | 回答的问题 | 系统输出 | 是否可由历史最终处置替代 |
|---|---|---|---|
| 证据事实 | 媒体与业务基准支持什么事实 | `positive / negative / review`、证据链、分数 | 否 |
| 成因与责任倾向 | 损伤何时发生、哪方更可能负责 | 原因候选、支持/反证、独立置信度 | 否 |
| 人工复审 | 是否必须由授权人员看原始证据 | `required / optional / not_required` | 否 |
| 经营处置 | 退款、补发、换货、补偿或拒绝 | 由甲方系统、SOP 和授权人员执行 | 否 |

事实层的 `positive` 只表示“现有证据支持本次事实诉求”，不是自动退款；`negative` 只表示“现有证据不支持本次事实诉求或命中甲方已批准的材料规则”，不是自动拒绝。平台责任、客户责任和经营弹性必须单列。

## 3. 三种业务现实

1. 平台责任倾向：证据链支持发货、包装、运输或商品原始状态问题。
2. 客户责任倾向：连续证据直接显示客户操作造成损伤，或完整证据不支持其主张。
3. 经营弹性：证据与责任都不足以唯一决定，但金额、用户关系或平台策略允许多种处置。该类应标记为 `discretionary_policy`，不能伪装成模型事实。

578028 没有完整开箱视频但最终获得补偿，说明“最终给方案”可能来自经营弹性，不能直接作为“视觉事实 positive”标签。588609 的旧标注把加速视为剪辑不合格，但甲方客服又说明一镜到底的加速视频可以慢放审核，说明历史标签存在口径漂移。两案均应进入黄金集复核，而不是直接训练规则。

## 4. 场景正确理解

### 4.1 商品有伤

- 首先判断伤情是否清晰可见、位置和严重程度；再独立判断发生阶段与责任倾向。
- 明确可见伤情可以形成事实 `positive`，即使物流、原包装或用户操作成因仍不确定。
- 只有连续动作前后证据直接显示用户造成损伤时，才可给客户责任倾向。
- “完整视频未见主诉伤情”的 `negative` 必须满足甲方批准的版本化门槛：诉求部位、同物关联、必检视角、可见覆盖、连续性、补充图片关联、媒体取证和置信度。
- 加速不等于拼接剪辑。一镜到底且关键过程可慢放观察时可继续审核；跳切、拼接、码流异常是独立风险信号。
- 主视频、补充图片和官方商品图是三种不同证据来源：主视频用于开箱过程与伤情可见性，补充图片用于特写佐证，官方图用于标准外观对照。三者不得在聚合时互相冒充。
- 不合规开箱视频可形成 SOP 层面的 `negative` 分类建议；若补充图显示瑕疵，报告必须同时写明“补充图支持瑕疵存在”和“补充图不能替代合规开箱视频”，并给出最低档补偿规则仅供甲方客服参考。
- Agent 在主视频中发现了人工未发现的疑似伤帧时，属于证据分歧，应展示该帧及前后片段并进入人工复核；不得用历史标签静默覆盖模型所见。

### 4.2 开箱视频合规

- 这是证据质量能力，不是独立售后责任结论。
- 分别输出抽帧首尾覆盖、媒体技术取证、开箱过程完整性和争议商品连续性；四者不得互相替代。
- 离镜秒数必须如实记录。超过配置阈值影响证据强度或触发补件，但不能单独证明调包、剪辑或欺诈。

### 4.3 发错货

- 需要订单 SKU、商品名、规格/款式/角色、数量和适用的随机/隐藏/特典规则作为应收基准。
- 用户媒体用于识别实收物；官方商品图只作为标准外观参考。
- 商品身份、订单基准和证据足够时可输出明确正负；缺唯一基准或随机规则时只能降级，不得靠视觉猜订单。

### 4.4 漏发货

- 必须对账“应发清单”和“视频已展示清单”，并核对包裹数、包裹-SKU 关系、物流状态、赠品/特典/套装规则。
- 一个物流单号不证明未分包，多个物流单号也不自动证明分包关系。
- 尚有包裹在途、视频未覆盖全部包裹/物品或包裹映射不明时，只能给证据不足；这是数据缺口，不是模型能力不足。

### 4.5 未成年人资料

- 该场景不是视频事实分类，必须分为材料存在性、跨材料字段一致性、甲方权威真实性核验三层。
- 年龄、监护关系、承诺书、订单支付和手机号实名字段可以做视觉一致性检查。
- 身份证、运营商实名、平台订单和支付真实性属于可选的权威核验层。当前甲方没有对应接口时默认关闭，不阻断视觉初审；未来可由甲方接口或授权人员开启严格核验。当前 POC 不得宣称已完成法定真伪验证。
- 五类必交材料按 SOP 核对：未成年人及监护人身份证明、监护关系证明、双方签字承诺书、订单/支付凭证、绑定手机号归属证明。户口本相关页或出生证明可按规则二选一。
- 缺少 EXIF、单个疑似编辑提示、部分字段看不清属于黄色非阻断风险；明确字段冲突、多重疑似编辑证据或处理失败才是红色阻断风险。
- 最终是否退款还涉及甲方政策、消费合理性与经营判断，不能由材料齐全直接推出。

## 5. 人工复审策略

| 等级 | 适用情况 | 建议动作 |
|---|---|---|
| 必须复审 | 服务失败、实质证据冲突、低于必须复审阈值、权威核验待完成 | 授权人员核对原始证据 |
| 可抽检 | 事实结论明确，但存在不影响事实的连续性、轻度取证、成因或责任不确定 | 按甲方风险偏好抽检 |
| 无需复审 | 事实证据链达到配置门槛且无阻断信号 | 甲方系统按已批准规则继续 |
| 先补材料 | 缺口可由用户补交解决 | 直接补件，不先占用人工席位 |

降低人工率不能靠放松发错/漏发的业务基准，也不能靠抬高模型分数。正确做法是把非阻断疑点降为“可抽检”，把真正材料缺口走“先补材料”，只把失败、冲突和权威核验放入“必须复审”。

## 6. 黄金样本集

先冻结 100 个视觉案件：商品有伤、开箱合规、发错货、漏发货各 25 个。未成年人资料因任务性质不同，另建材料审核集。

每个黄金案件至少包含：

- 稳定案件 ID、场景、诉求范围与标签版本；
- 允许进入推理的用户媒体、最小化订单/SKU、物流/包裹和审核时点前对话；
- 不进入推理的人工标注、仓库终判、最终补偿/退款结果；
- `evidence_fact_label`：证据支持、证据不支持或证据不足；
- `responsibility_tendency`：平台、客户、不确定；
- `business_disposition`：历史处置及是否属于经营弹性；
- 事实理由、关键帧/材料引用、证据缺口和适用 SOP 版本。

黄金集不要求把所有历史案例包装成绝对客观真相。标注者无法一致判断时应明确记为 `review` 或 `discretionary_policy`，不强迫二选一。

## 7. 评测与验收

- 工程成功率、失败率、延迟、模型调用数、Token 与估算成本；
- 事实三分类混淆矩阵、正负召回、确定性覆盖率和人工复审率；
- `required / optional / not_required / request_more_material` 分布；
- 按场景、证据完备性和策略版本分层结果；
- 报告标题、事实结论、复审等级、流程建议和视频论证的矛盾次数；
- 目录名、`annotation.json`、`reply.json` 和最终处置进入模型请求的次数。

在黄金集冻结前，只能称为 Demo 工程回归，不能宣称生产准确率。冻结后再根据结果校准置信度阈值；当前分数仍是未校准证据强度，不是客观正确率。

## 8. 接口与部署边界

- 甲方业务系统统一调用 `/api/v1/review/*`；网页工作台用于 UAT、人工复测和阅读报告。
- 路径由调用方决定，服务端只接收 multipart 文件或未来双方确认的对象存储引用，不依赖我方 `E:` 盘。
- `include_html_report=false` 可只取结构化字段；开启时返回同一任务的 HTML 报告。
- 真实包裹-SKU 映射、仓库核验、对象存储/转码回调仍是 `customer_integration_required`。未成年人权威核验仅在甲方显式启用严格策略时是 `customer_integration_required`，默认视觉初审不依赖它。

## 9. 本轮代码修正

- 商品有伤：可见伤情事实与损伤成因分离；成因不确定不再强制覆盖明确伤情事实。
- 补充图片：审核通道可以单独确认“补充图中可见伤情”，但不得据此改写“主视频是否看见伤情”；同物关联、形成时间和责任仍分别判断。
- SOP 分类：服务端批准策略可将“经完整时间轴确认的不合规开箱视频”映射为 `negative` 审核倾向；加速字段不参与该规则，补充图片只生成最低档补偿或复核参考。
- 连续性：商品连续性不确定只降低成因/责任可信度；发错、漏发和完整视频未见伤情仍保留严格连续性门槛。
- 复审：连续性不确定作为可抽检信号，不再自动变成必须复审。
- 报告：首屏先展示“按照 SOP 的审核倾向”，再展示“建议进一步评估”；主视频和补充图片分别呈现，公开页不显示内部策略编号。
- 视频论证：明确解释抽帧、媒体取证、开箱完整性和商品连续性四个维度，并把内部状态码转换成人类可读文案。

## 10. 本轮验收证据

- 同一真实案件 `588609` 通过正式 API 三次盲测，均只上传用户诉求、3 份媒体和安全订单快照，未上传目录标签、`annotation.json`、`reply.json` 或 `manifest.json`。
- 首次任务 `RJ-7213A46CF99E4AAC` 暴露旧建议对象内部矛盾；第二次任务 `RJ-862F25E4E8E04FAB` 证明同一材料的模型伤情存在性会在 `confirmed / uncertain` 间波动，但其 `adopted_evidence` 已稳定记录补图伤情、0.90 证据分和同物关联。
- 最终任务 `RJ-765B0EA12FB04FE9` 返回 `positive / 0.65`、伤情 `confirmed`、成因 `indeterminate`、人工复审 `optional`、流程 `continue_by_customer_policy`；媒体取证完成，HTML 报告可读取且不含“需要VIP客服复核”或内部媒体状态码。
- 自动验证：视觉审核 145/145、审核服务 79/79、私有化 API 14/14、Python 编译和前端生产构建通过。API 报告：`tests/reports/private_deployment_api_smoke_20260727_183126.json`。

## 11. 剩余风险与唯一下一入口

本轮已完成机制性全 REVIEW、建议对象矛盾和报告口径的工程闭环；这不等于 23 个历史样本已达到生产准确率。历史标签仍有口径漂移，继续全量复跑只能得到 Demo 分布，不能得到可信准确率。下一步唯一入口是由双方冻结 100 案黄金集，再以正式 API 输出按场景的三分类混淆矩阵、确定性覆盖率和人工复审率并校准阈值。若仍出现异常高 REVIEW，应按缺口代码区分业务输入不完备、模型证据不足与工程失败，禁止再用单一“模型保守”解释。
