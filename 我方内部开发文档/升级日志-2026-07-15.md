# 升级日志 · 2026-07-15

## 0714 客户反馈闭环

- 新增 `specs/002-customer-feedback-0714-closure/`，将反馈映射到规格、数据模型、接口契约、任务和验收清单。
- 新增 `scripts/check_customer_agent_0714_regression.py`，覆盖业务意图、订单归属、附件语义、审核回写、卡片、人工转接和坐席队列。
- 修复用户切换竞态：会话代际、AbortController、焦点订单和附件组件同步失效。
- 修复抽赏“概率/保底/发货时效”被误判物流，并从多订单中定位抽赏活动事实。
- 修复附件审核摘要中的人工复核文字误触发会话转人工。
- 修复“外包装”被“外包”防泄漏规则误伤及前端错误替换。
- 修复管理员 `scope=all` 队列接口引用不存在角色导致 500。
- 人工排队期间允许用户继续补充消息；自动欢迎语不计入真人首响。
- VIP 客服简报增加用户订单、画像和最近审核任务；队列支持本人、可接和全队列权限。

## 审核服务

- 聊天审核任务增加 `client_case_id`、`order_id` 和结构化 `context`。
- 历史审核任务可按用户/会话回填客服上下文。
- 大文件仍以 `/api/v1/review/jobs` 为标准入口；超过聊天上传边界时返回对象存储与转码建议。
- 视觉服务公开媒体目录收敛，不再暴露仓库根目录。
- 工作台支持 0.2/0.5/1/2 FPS，报告链接独立打开且不触发父级事件。
- `sample_003` 真实批次验收通过：16 文件、约 106 MB、置信度 0.95、成本估算与 HTML 报告完整。

## 发布工程

- `.venv/` 纳入 Git 忽略，避免依赖环境误提交和 ZIP 膨胀。
- 预发布脚本自动识别 `.venv`/`venv`，并新增 0714、前端隔离和数据目录隔离门禁。
- 内部包安全复制 `sample_labels.json`，仅保留报告侧评估字段，明确 `send_to_model=false`。
- 全链路 E2E 更新为排队期间允许补充消息，并使用管理员 Token 清理测试会话。
- 390×844 浏览器门禁增加横向溢出检查，修复移动端品牌标签断字。
- 新增甲方人类可读 HTML 更新日志，逐项映射 0714 反馈、代码实现、验收证据和外部联调边界。
- 交付固定拆分为内部研发源码包与甲方演示测试包；两包使用同一提交号和证据哈希，避免源码、环境配置或数据库误发甲方。

## 环境配置核验

- 核心对话模型要求 `SENSENOVA_API_KEY` 或 `OPENAI_API_KEY` 至少一个有效；当前本地环境两项均为 `SET`。
- 视觉审核要求 `VISION_REVIEW_API_KEY`、`GEMINI_API_KEY` 或 `GOOGLE_API_KEY` 至少一个有效；当前 `GEMINI_API_KEY=SET`，已满足主审链路。
- `GOOGLE_API_KEY`、`ARK_API_KEY`、`VISION_REVIEW_API_KEY` 及其他兼容网关 Key 是可选备用通道，未选用时允许为空。
- `MITAKO_JWT_SECRET` 当前未配置，本地 POC 可使用进程级随机密钥；生产部署必须注入非默认强密钥后再开启正式鉴权。
- `.env` 不进入 Git 或甲方包，只进入受控内部研发包；`.env.example` 只保留变量名和空值模板。

## 当前外部依赖

企业微信、飞书、真实订单/库存、CRM/CDP、甲方客服平台、对象存储、云转码和 120GB 真值评测仍需甲方提供接口、凭证、测试环境和脱敏数据。代码中必须继续标为 `customer_integration_required` 或 `integration_status=not_connected`。
