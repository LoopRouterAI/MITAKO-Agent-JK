# 客户验证包打包说明

本项目对外只发送由脚本生成的客户验证包，不手工压缩源码仓库。

## 打包前检查

1. 已通过 `npm run build`。
2. 已通过核心 Python 编译和 smoke 验证。
3. 已确认客户可见文档位于 `甲方沟通交付文档/`，我方内部实现细节位于 `我方内部开发文档/`。
4. 不发送源码仓库、`.env`、本地数据库、临时目录、测试报告目录或运行时记忆。
5. 旧版陪伴/角色扮演代码只保留在 archive，不进入当前客户验证范围。

## 打包命令

在项目根目录执行：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File scripts\package_release.ps1
```

输出文件只位于项目的 `dist/`：

```text
dist/MITAKO_Agent-customer-preview-YYYYMMDD.zip
dist/MITAKO_Agent-customer-delivery.html
```

不要把交付文件复制到项目父目录、`release/` 或其他临时目录。内部研发包同样只生成到 `dist/`，但不得发送甲方。

## 交付边界

客户验证包用于展示、试用和接口联调准备。接入甲方真实 Java 后台前，必须先完成接口契约确认、账号权限确认、回调签名验证、正负样本盲测和上线回滚方案。
