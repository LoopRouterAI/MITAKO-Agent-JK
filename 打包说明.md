# 打包说明（维护方）

> **请勿把 `venv/`、`node_modules/`、`.env` 打进 ZIP。** 接收方自行 `setup_venv.bat` + `npm install`。

## 打包前检查

1. 根目录 `开发上手.md`、`测试指南.md`、`README.md` 已更新  
2. 已跑通：`双系统测试-手工UAT-Windows.bat` 或冒烟通过  
3. `.env` **不要**打包（仅 `.env.example`）  
4. 可选：先 `npm run build` 确保 `dist/` 最新  

## 应包含

- 源码：`*.py` `src/` `auth/` `scripts/` `tests/` `tools/` `docs/` `specs/` 或 `docs/archive/specs/`
- 配置：`package.json` `requirements.txt` `.env.example` `vite.config.js`
- 启动：`setup_venv.bat` `一键启动-Windows.bat` 根目录测试 BAT
- 预构建（推荐）：`dist/`（接收方可跳过首次 build 加快速度）

## 必须排除

| 路径 | 原因 |
|------|------|
| `venv/` | 本地 Python 环境，体积大 |
| `node_modules/` | `npm install` 重建 |
| `.env` | 含密钥 |
| `scratch/` | 临时脚本 |
| `__pycache__/` | 缓存 |
| `.codegraph/` `.cursor/` `.specify/` | 工具/IDE |
| `tests/reports/*.html` | 历史报告 |
| `data/*.db` | 本地数据库（接收方 seed 重建） |
| `PPT-*` | 演示材料 |

## 打包命令（你说「可以打包了」后执行）

在项目根目录 PowerShell：

```powershell
.\scripts\package_release.ps1
```

输出：`MITAKO_Agent-release-YYYYMMDD.zip`（与项目同级目录）

## 接收方快速启动

```bat
解压 MITAKO_Agent-release-*.zip
cd MITAKO_Agent
阅读 开发上手.md
setup_venv.bat
npm install
copy .env.example .env
REM 编辑 .env 填入 LLM Key
一键启动-Windows.bat
```
