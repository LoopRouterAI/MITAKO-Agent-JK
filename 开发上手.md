# MITAKO Agent 开发上手

打开项目后建议先阅读本文件。测试验收请看 [测试指南.md](./测试指南.md)，代码边界请看 [docs/CodeWiki.md](./docs/CodeWiki.md)。

## 当前系统边界

| 模块 | 页面/接口 | 核心代码 |
| --- | --- | --- |
| 用户客服 Agent | `/`、`/api/v1/chat/stream` | `agent.py`、`business_api.py`、`business_mock_service.py` |
| 人工客服工作台 | `/desk`、`/api/v1/handoff/*` | `handoff_service.py`、`handoff_store.py`、`src/desk/` |
| 运营后台 | `/admin`、`/api/v1/admin/*` | `admin_service.py`、`admin_store.py`、`src/admin/` |
| 视觉审核工作台 | `http://127.0.0.1:7861/` | `poc/visual_review_poc/` |
| 甲方联调实验室 | `tools/partner_lab/` | Mock IdP、Mock IM、Mock 业务接口 |

旧版陪伴、文字冒险、角色扮演服务线已经封存到 `archive/companion_roleplay_mode_20260705/`。主系统不再提供 `/companion`、`/companion-desk` 或 `/api/v2/companion/*`。

## 首次环境

```bat
cd MITAKO_Agent
python -m venv venv
venv\Scripts\python.exe -m pip install -r requirements.txt
npm install
copy .env.example .env
```

编辑 `.env` 填入需要的 LLM/视觉审核 API Key。首次跑浏览器 E2E 时执行：

```bat
venv\Scripts\playwright install chromium
```

## 日常开发

```bat
一键启动-Windows.bat
```

| 入口 | URL | 测试账号 |
| --- | --- | --- |
| 用户客服端 | http://127.0.0.1:8000/ | 无需登录 |
| 人工客服工作台 | http://127.0.0.1:8000/desk | desk0816 / desk123 |
| 运营后台 | http://127.0.0.1:8000/admin | admin / admin123 |
| 视觉审核工作台 | http://127.0.0.1:7861/ | 本地 POC |

改前端后运行 `npm run build`。改 Python 后重启 `main.py` 或重新运行一键启动脚本。

## 甲方接口联调

甲方默认 Java 技术栈，我方以 REST/JSON、JWT/OIDC、HMAC 回调签名和幂等键为主契约。对接说明分两套：

| 文档 | 用途 |
| --- | --- |
| `甲方沟通交付文档/index.html` | 给甲方 CEO、客服负责人、Java 开发直接浏览 |
| `甲方沟通交付文档/客服Agent与视觉审核对接指南.md` | 甲方接口字段、流程和联调清单 |
| `我方内部开发文档/index.html` | 给我方研发查看系统设计、队列、适配层和验收方法 |
| `我方内部开发文档/客服Agent视觉审核系统设计指南.md` | 我方实现细节和后续改造计划 |

联调实验室：

```bat
tools\partner_lab\联调-MITAKO对接模拟终端-Windows.bat
```

说明见 [docs/delivery/integration-lab.md](./docs/delivery/integration-lab.md)。

## 生产/严格鉴权

生产必须启用鉴权、替换 JWT 密钥并关闭本地绕过：

```env
MITAKO_AUTH_REQUIRED=1
MITAKO_PROTECTED_API_AUTH_REQUIRED=1
MITAKO_DEV_AUTH_BYPASS=0
MITAKO_JWT_SECRET=<生产随机长密钥>
MITAKO_SSO_DEMO=0
```

完整清单见 [docs/security/production-checklist.md](./docs/security/production-checklist.md)。
