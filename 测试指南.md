# MITAKO Agent 测试指南

开发环境搭建见 [开发上手.md](./开发上手.md)。

## 当前测试范围

| 模块 | 前台 | 后台 | 核心场景 |
| --- | --- | --- | --- |
| 客服 Agent | `/` | `/desk`、`/admin` | AI 对话、SOP 分流、转人工、坐席接单、后台配置 |
| 视觉审核工作台 | `http://127.0.0.1:7861/` | 工作台右侧队列/报告 | 三类审核入口、样本文件夹、抽帧、报告、失败重试 |
| 甲方联调实验室 | `tools/partner_lab/` | Mock IdP/IM/业务接口 | OIDC、IM 事件、业务查询、回调契约 |

旧版陪伴、角色扮演和文字冒险测试已经随服务线封存，不再作为当前回归范围。

## 常用脚本

| 脚本 | 用途 |
| --- | --- |
| `一键启动-Windows.bat` | 构建并启动本地开发环境 |
| `scripts\run_all_e2e.bat` | 当前主链路自动化回归 |
| `venv\Scripts\python.exe scripts\dual_system_smoke_test.py` | 主系统三端 smoke |
| `venv\Scripts\python.exe scripts\check_visual_workbench_smoke.py` | 视觉审核工作台 smoke |
| `tools\partner_lab\联调-MITAKO对接模拟终端-Windows.bat` | 甲方接口联调模拟 |

## 测试账号

基址：`http://127.0.0.1:8000`

| 端 | 账号 | 密码 |
| --- | --- | --- |
| `/desk` | desk0816 | desk123 |
| `/admin` | admin | admin123 |
| `/admin` 审批 | supervisor | super123 |

## 手工 UAT

1. 用户端发送“我的订单清关太慢了，能不能催一下？”确认客服 Agent 能安抚情绪、解释边界并给出下一步。
2. 触发转人工，确认用户端出现排队状态，`/desk` 能接单并回复。
3. 在 `/admin` 检查队列、路由、补偿审批和报表导出。
4. 打开视觉审核工作台，分别测试开箱视频、商品有伤、未成年人资料审核三个入口。
5. 在视觉审核报告中确认结论优先、证据链清晰、图片可预览、时间戳可追溯、低置信度进入人工复核。

## 发版前通过标准

- `npm run build` 通过。
- 核心 Python 文件 `py_compile` 通过。
- `scripts\check_visual_workbench_smoke.py` 通过。
- 主系统入口 `/`、`/desk`、`/admin` 可访问；旧 `/companion` 入口不可作为当前产品访问。
- 甲方版 HTML/MD 文档不暴露模型供应商、API Key、内部 prompt、token 成本或调试细节。
